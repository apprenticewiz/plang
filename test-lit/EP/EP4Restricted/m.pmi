{ plang module interface: m }
module m interface;
export m = (handle, seth, showh);
type rep = record
n: integer;
end;
type handle = restricted rep;
procedure seth(var h: rep; v: integer);
procedure showh(h: rep);
end.

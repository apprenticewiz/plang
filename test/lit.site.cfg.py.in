# lit.site.cfg.py -- GENERATED by CMake from lit.site.cfg.py.in. Do not edit
# the generated copy; edit this template or lit.cfg.py instead.
#
# Everything build-tree-specific (where plang landed, where LLVM's own tools
# live) is substituted here, at configure time, so lit.cfg.py itself stays a
# plain, portable file with no CMake-generated content mixed into it.

import os

config.plang_source_dir = r"@PLANG_SOURCE_DIR@"
config.plang_binary_dir = r"@PLANG_BINARY_DIR@"
config.plang_tools_dir  = r"@PLANG_TOOLS_DIR@"
config.llvm_tools_dir   = r"@LLVM_TOOLS_BINARY_DIR@"

lit_config.load_config(
    config, os.path.join(config.plang_source_dir, "test", "lit.cfg.py"))
